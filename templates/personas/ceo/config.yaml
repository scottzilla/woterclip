name: CEO
role: orchestrator
label: null
is_default: true
escalates_to: board

required_tools:
  - mcp__claude_ai_Linear          # Prefix match — covers all Linear MCP tools

runtime:
  model: sonnet                    # Triage is lightweight — no need for Opus
  thinking_effort: medium
  max_turns: 100
  enable_chrome: false
  timeout: 0
  extra_args: []
